main var a,b,c,d,e; {
	let a <- 2;
	let b <- a+2;
	let c <- a+b;
	let d <- a+b;
	let d <- d + 10;
	let e <- a+2;
	let e <- e*2+a;
	while a > 0 do
		while b > 0 do
		    while c > 0 do
			    if e > a
			    then
			        let e <- e+1;
			    else
			        let a <- a+3;
			    fi;
			od;
		od;
            let d<-a+2;
            let d<-e+2;
	od;
	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
	call OutputNum(d);
	call OutputNum(e);
	}.