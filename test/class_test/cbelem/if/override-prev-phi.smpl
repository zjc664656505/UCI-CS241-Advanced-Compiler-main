main
    var a, b; {
        let a <- call InputNum();
        if a < 10
        then
            let b <- a;
        else
            let a <- (a + a);
            let a <- a * a;
        fi;
    }.
